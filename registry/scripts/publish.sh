#!/usr/bin/env bash
# Publish a plugin to the registry index
set -euo pipefail

PLUGIN_DIR="${1:?Usage: publish.sh <plugin-dir>}"

echo "Publishing plugin from: $PLUGIN_DIR"
echo "TODO: Implement registry publishing"
