//! ResponseEntity transforms to gin.Context responses.

use jovial_plugin::prelude::*;

/// Transform ResponseEntity returns into gin.Context JSON/status responses.
pub fn transform_response(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
