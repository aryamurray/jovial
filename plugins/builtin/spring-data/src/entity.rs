//! @Entity → GORM model struct transforms.

use jovial_plugin::prelude::*;

/// Transform a JPA @Entity class into a GORM model struct.
pub fn transform_entity(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
