//! CrudRepository → GORM query transforms.

use jovial_plugin::prelude::*;

/// Transform a Spring Data repository interface into GORM query methods.
pub fn transform_repository(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
