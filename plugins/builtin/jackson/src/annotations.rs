//! @JsonProperty → struct tag transforms.

use jovial_plugin::prelude::*;

/// Transform Jackson annotations into Go struct tags.
pub fn transform_annotation(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
