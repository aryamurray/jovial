//! ObjectMapper → json.Marshal/Unmarshal transforms.

use jovial_plugin::prelude::*;

/// Transform ObjectMapper calls to encoding/json equivalents.
pub fn transform_object_mapper(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
