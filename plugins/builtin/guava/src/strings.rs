//! Strings utility transforms.

use jovial_plugin::prelude::*;

/// Transform Guava Strings utilities to Go string functions.
pub fn transform_strings(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
