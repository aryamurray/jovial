//! HttpClient creation and configuration transforms.

use jovial_plugin::prelude::*;

/// Transform HttpClient builder patterns to Go http.Client.
pub fn transform_client(_node: &JavaNode) -> Result<Vec<GoNode>, PluginError> {
    todo!()
}
